import{_ as a}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as t,e as o,o as r}from"./app-DqTKVB4L.js";const n={};function i(s,e){return r(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="defense-in-depth-hardening-measures" tabindex="-1"><a class="header-anchor" href="#defense-in-depth-hardening-measures"><span>Defense-in-Depth Hardening Measures</span></a></h1><p>For DSF 2.1.0 we performed a number of IT security hardening measures. The following changes follow security best practices. They reduce risk for non-standard deployments, protect against future code changes that could introduce new attack paths, and align with common security audit expectations.</p><p>Each entry describes the change and why we consider it not exploitable in the standard DSF deployment model.</p><h2 id="deployment-context" tabindex="-1"><a class="header-anchor" href="#deployment-context"><span>Deployment Context</span></a></h2><p>The standard DSF deployment uses:</p><ul><li><strong>Apache2 reverse proxy</strong> with <code>mod_ssl</code> performing TLS termination and client certificate validation (<code>SSLVerifyClient require</code>, DN checks against trusted CA list)</li><li><strong>Docker containers</strong> with network isolation (frontend / backend / inter-service networks)</li><li><strong>Jetty application</strong> on internal port 8080, not directly reachable from outside</li><li><strong>Status port</strong> on <code>127.0.0.1:10000</code> (localhost only), separate Jetty connector</li><li><strong>Configuration</strong> via Docker environment variables / secrets, only admin-writable</li><li><strong>PostgreSQL</strong> not exposed to host, only accessible from the application container</li></ul><p>The assessments below consider this standard deployment. Non-standard deployments (e.g., Jetty directly exposed, no reverse proxy) may have different risk profiles.</p><h2 id="improved-xml-transformer-configuration" tabindex="-1"><a class="header-anchor" href="#improved-xml-transformer-configuration"><span>Improved XML Transformer Configuration</span></a></h2><p>Disabled external entity resolution and enabled secure processing on <code>TransformerFactory</code> instances in <code>ProcessService</code>, <code>ThymeleafTemplateServiceImpl</code>, and <code>BundleGenerator</code>. We consider this not exploitable because although external organizations and authorized users can create FHIR resources (e.g., Tasks), the content never reaches the TransformerFactory as raw XML. The data path is: user input is parsed by HAPI FHIR into a Java object model (stripping any XML-level constructs like DOCTYPE/ENTITY declarations), stored as JSONB in PostgreSQL, and then re-serialized to well-formed XML by HAPI&#39;s serializer when displayed. The TransformerFactory only pretty-prints HAPI&#39;s serialized output, where all text content is properly XML-escaped. <code>BundleGenerator</code> is a Maven build-time tool, not a runtime component. Hardening against CWE-611 (XXE).</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/b89c512dc" target="_blank" rel="noopener noreferrer"><code>b89c512dc</code></a></p><h2 id="database-username-group-validation" tabindex="-1"><a class="header-anchor" href="#database-username-group-validation"><span>Database Username/Group Validation</span></a></h2><p>Added regex validation (<code>^[a-zA-Z_][a-zA-Z0-9_$]{0,62}$</code>) for PostgreSQL identifier config parameters used in DDL statements. We consider this not exploitable because these values come exclusively from admin-managed config files and Docker environment variables — an attacker with write access to these already has full system control. Hardening against CWE-89.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/7e2de0ed8" target="_blank" rel="noopener noreferrer"><code>7e2de0ed8</code></a></p><h2 id="https-enforcement-for-oidc-endpoints" tabindex="-1"><a class="header-anchor" href="#https-enforcement-for-oidc-endpoints"><span>HTTPS Enforcement for OIDC Endpoints</span></a></h2><p>Added explicit check that OIDC token and JWKS endpoints use HTTPS. We consider this not exploitable because a compromised OIDC provider can already issue arbitrary tokens directly, making HTTP interception redundant. In the MITM scenario (legitimate provider, network attacker), the provider URL is admin-configured as HTTPS and legitimate providers always return HTTPS endpoints in their discovery documents. In both cases, obtained tokens are constrained by the admin-managed <code>roleConfig</code> — no privilege escalation beyond configured roles is possible. Hardening against CWE-918 / CWE-319.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/bdeddf308" target="_blank" rel="noopener noreferrer"><code>bdeddf308</code></a></p><h2 id="jwks-minimum-key-size-and-use-sig-filtering" tabindex="-1"><a class="header-anchor" href="#jwks-minimum-key-size-and-use-sig-filtering"><span>JWKS Minimum Key Size and <code>use=sig</code> Filtering</span></a></h2><p>Added 2048-bit minimum RSA key length and <code>use=sig</code> check for JWKS keys used in JWT verification. We consider this not exploitable because no mainstream OIDC provider (Keycloak, etc.) publishes RSA keys below 2048 bits. Even if an attacker could forge tokens via weak keys, privileges are constrained by the admin-managed <code>roleConfig</code> — the same limitation as with a compromised OIDC provider. Hardening against CWE-326 / CWE-347.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/31c2e974d" target="_blank" rel="noopener noreferrer"><code>31c2e974d</code></a></p><h2 id="removed-access-token-claim-logging" tabindex="-1"><a class="header-anchor" href="#removed-access-token-claim-logging"><span>Removed Access Token Claim Logging</span></a></h2><p>Removed DEBUG-level logging of token claims (groups, roles) in <code>AbstractIdentityProvider</code> and <code>BearerTokenAuthenticator</code>. We consider this not exploitable because DEBUG logging is disabled in production by default, and the logged data (group memberships, roles) is operational metadata — not secrets like passwords or signing keys. Hardening against CWE-532.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/cc0bb71ef" target="_blank" rel="noopener noreferrer"><code>cc0bb71ef</code></a></p><h2 id="session-cookie-security-attributes" tabindex="-1"><a class="header-anchor" href="#session-cookie-security-attributes"><span>Session Cookie Security Attributes</span></a></h2><p>Added <code>Secure</code>, <code>HttpOnly</code>, and <code>SameSite=LAX</code> flags to session cookies. We consider this not exploitable because TLS is terminated at the Apache HTTP Server reverse proxy (clients only communicate over HTTPS), the <code>HttpOnly</code> flag is only relevant if an XSS vulnerability exists (none is exploitable, see <code>th:utext</code> below), and <code>SameSite</code> has limited impact since primary authentication uses mTLS, not cookies. Hardening against CWE-614 / CWE-1004.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/5cc00611b" target="_blank" rel="noopener noreferrer"><code>5cc00611b</code></a></p><h2 id="json-serialization-in-jsonb-query-parameters" tabindex="-1"><a class="header-anchor" href="#json-serialization-in-jsonb-query-parameters"><span>JSON Serialization in JSONB Query Parameters</span></a></h2><p>Replaced string concatenation with Jackson <code>ObjectMapper</code> serialization for JSONB query parameters in all DAO classes and search filters. We consider this not exploitable because user-controlled values are bound via <code>PreparedStatement.setString()</code>, which transmits query template and parameter values separately to PostgreSQL — the parameter value is never parsed as SQL and there is no way to inject <code>JOIN</code>, <code>UNION</code>, or subqueries. Within the JSONB domain, the identity filter (access control) operates as a separate <code>AND</code> condition with server-side parameters that cannot be influenced by search parameter manipulation, the <code>@&gt;</code> containment operator becomes more restrictive with additional injected elements (not less), and result sets are always a subset of authorized resources — no enumeration of unauthorized data is possible. Hardening against CWE-89.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/37c0282c6" target="_blank" rel="noopener noreferrer"><code>37c0282c6</code></a></p><h2 id="client-certificate-expiration-check" tabindex="-1"><a class="header-anchor" href="#client-certificate-expiration-check"><span>Client Certificate Expiration Check</span></a></h2><p>Added explicit certificate expiration check and changed auth type from <code>&quot;RSA&quot;</code> to <code>&quot;UNKNOWN&quot;</code> in <code>ClientCertificateAuthenticator</code>. We consider this not exploitable because the Apache HTTP Server reverse proxy performs TLS termination with <code>SSLVerifyClient require</code> and validates certificate expiration, chain trust, and revocation before requests reach Jetty. The Jetty-level check is a redundant second validation. Hardening against CWE-295 / CWE-324.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/c6d369828" target="_blank" rel="noopener noreferrer"><code>c6d369828</code></a></p><h2 id="safe-yaml-deserialization" tabindex="-1"><a class="header-anchor" href="#safe-yaml-deserialization"><span>Safe YAML Deserialization</span></a></h2><p>Replaced <code>new Yaml()</code> with <code>new Yaml(new SafeConstructor(new LoaderOptions()))</code> in <code>RoleConfigReader</code>. We consider this not exploitable because the YAML content comes exclusively from admin-managed config files (<code>config.properties</code>) and Docker environment variables — an attacker with write access to these can already execute arbitrary code through simpler means. Hardening against CWE-502.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/e0b6cf00a" target="_blank" rel="noopener noreferrer"><code>e0b6cf00a</code></a></p><h2 id="thymeleaf-th-utext-replaced-with-th-text" tabindex="-1"><a class="header-anchor" href="#thymeleaf-th-utext-replaced-with-th-text"><span>Thymeleaf <code>th:utext</code> Replaced with <code>th:text</code></span></a></h2><p>Replaced <code>th:utext</code> (unescaped HTML output) with <code>th:text</code> / <code>th:each</code> for page headings built from URL segments. We consider this not exploitable because the old code applied <code>HtmlUtils.htmlEscape()</code> to all user inputs before concatenation — this escapes <code>&amp; &lt; &gt; &quot; &#39;</code>, preventing tag injection (<code>&lt;</code> <code>&gt;</code> escaped to <code>&amp;lt;</code> <code>&amp;gt;</code>) and attribute breakout (<code>&quot;</code> escaped to <code>&amp;quot;</code>, which per HTML spec does not terminate double-quoted attributes). The fix makes XSS structurally impossible and removes reliance on manual escaping — a future developer could have forgotten the <code>htmlEscape()</code> call when adding new URL segments. Hardening against CWE-79.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/58edc26c5" target="_blank" rel="noopener noreferrer"><code>58edc26c5</code></a></p><h2 id="error-message-removed-from-status-endpoint" tabindex="-1"><a class="header-anchor" href="#error-message-removed-from-status-endpoint"><span>Error Message Removed from Status Endpoint</span></a></h2><p>Removed database exception messages from the status endpoint error response body. We consider this not exploitable because the status endpoint runs on a separate Jetty connector (port 10000, bound to <code>127.0.0.1</code> only) with role-based access control (<code>STATUS_PORT_ROLE</code>) — it is not reachable through the Apache HTTP Server reverse proxy or from outside the Docker container. Hardening against CWE-209.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/5103d0e9d" target="_blank" rel="noopener noreferrer"><code>5103d0e9d</code></a></p><h2 id="content-security-policy-and-binary-inline-display" tabindex="-1"><a class="header-anchor" href="#content-security-policy-and-binary-inline-display"><span>Content Security Policy and Binary Inline Display</span></a></h2><p>Introduced a new inline display mode for <code>Binary</code> resources with a strict CSP (<code>script-src &#39;none&#39;</code>), a media type policy, and <code>ContentTypeSanitizer</code>. We consider this not a vulnerability fix because the old code served all <code>Binary</code> resources with <code>Content-Disposition: attachment</code> (forced download, never rendered in browser), so the old <code>unsafe-inline</code> CSP was never reachable. This commit implements a new feature with secure defaults. Hardening against CWE-1021.</p><p>Commit: <a href="https://github.com/datasharingframework/dsf/commit/454c1197b" target="_blank" rel="noopener noreferrer"><code>454c1197b</code></a></p>',43)])])}const l=a(n,[["render",i]]),p=JSON.parse('{"path":"/operations/latest/hardening-measures.html","title":"Security Hardening Measures","lang":"en-US","frontmatter":{"title":"Security Hardening Measures","icon":"safe"},"git":{},"readingTime":{"minutes":3.97,"words":1192},"filePathRelative":"operations/latest/hardening-measures.md","excerpt":"\\n<p>For DSF 2.1.0 we performed a number of IT security hardening measures. The following changes follow security best practices. They reduce risk for non-standard deployments, protect against future code changes that could introduce new attack paths, and align with common security audit expectations.</p>"}');export{l as comp,p as data};
